{
 "category": "Sources",
 "kind": "laser_divergent",
 "label": "Divergent laser",
 "params": {
  "diameter": {
   "default": 10.0,
   "help": "emit aperture diameter (circular) or edge length (rectangular, round_flag=0)",
   "unit": "mm"
  },
  "length": {
   "default": 10.0,
   "help": "housing length",
   "unit": "mm"
  },
  "roc": {
   "default": 200.0,
   "help": "emit cap radius of curvature (divergence = diameter/2/roc); only applies when round_flag=1",
   "unit": "mm"
  },
  "round_flag": {
   "default": 1,
   "help": "1 = circular (spherical emit cap), 0 = flat rectangular emitter",
   "unit": ""
  }
 },
 "props": {
  "coherent": true,
  "lambdac": 633.0,
  "power": 5.0
 },
 "tooltip": "Laser with a convex spherical emit cap: emitted rays diverge from a virtual point (radius of curvature = roc). With round_flag=0, a flat rectangular emit face is built instead (roc does not apply)."
}