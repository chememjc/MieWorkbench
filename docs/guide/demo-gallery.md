# Demo gallery

*Skeleton — Phase B fills this in once the current demo rebuild lands.*

`demos/` holds 35 classic-system showcase scenes (`.MieWB`), built through
the Project/chain op path (`scripts/make_demos.py`); a subset is gated by
the placement+power equivalence oracle
(`scripts/run_demo_equivalence.py`, `demos/baselines/`). See
[`../../demos/README.md`](../../demos/README.md) for prescriptions and
citations, and [walkthroughs/](walkthroughs/README.md) for the
(not-yet-written) per-demo GUI walkthrough pages this page will link to.
