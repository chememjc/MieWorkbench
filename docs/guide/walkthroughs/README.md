# Demo walkthroughs (Phase B)

Reserved. Each page here will pair a showcase demo's rendered gallery
image (`demos/gallery/<name>.png`, ParaView-rendered) with the GUI steps
that build/reproduce it — outliner + train-editor + results screenshots
captured via `scripts/tools/capture_docs_screenshots.py --scene-root
demos/`.

Not written yet: the demo set is being rebuilt on another branch as of
this round. See [`../demo-gallery.md`](../demo-gallery.md) for the
current skeleton and `demos/README.md` for the prescriptions.

Candidate pages (pick 4 once the rebuild lands and the showcase set is
final): a two-element imaging system (e.g. `camera_triplet` or
`microscope_objective`), an interferometer (`michelson`), a dispersive
system (`prism_spectrometer` or `czerny_turner`), and a folded/reflective
system (`newtonian` or `schmidt_cassegrain`).
